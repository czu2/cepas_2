<div class="container mt-5">

    <h2>Nuevo Enólogo</h2>

    <%= form_with(model: @oenologist, local: true) do |f| %>

        <div class="border rounded bg-light p-3">
            <div class="mb-3">
                <%= f.text_field :name, placeholder:'Nombre', size:'50px' %>
            </div>

            <div class="mb-3">
                <%= f.number_field :age, placeholder:'Edad' %>
            </div>

            <div class="mb-3">
                <%= f.text_field :nationality, placeholder:'Nacionalidad', size:'50px' %>
            </div>
        </div>


        <div class="border rounded bg-light p-3 mt-3">
            <h3>Revistas</h3>
            <p class="text-info">Selecciona la revista y las posiciones del enólogo</p>
            <div class="field">
            <table class="table">
                <thead>
                    <th>Revista</th>
                    <th>Posición</th>
                </thead>

                <tbody>
                    <%= f.collection_check_boxes(:magazine_ids, Magazine.all, :id, :name) do |box| %>
                        <%= box.label class:"label-chechbox" do %>
                            <tr>
                                <td>
                                    <%= box.check_box + ' ' + box.text %>
                                </td>
                                <td>
                                    <span class="mr-3">
                                        <%= box.check_box + ' Editor' %>
                                    </span>
                                    <span class="mr-3">
                                        <%= box.check_box + ' Writer' %>
                                    </span>
                                    <span>
                                        <%= box.check_box + ' Reviewer' %>
                                    </span>
                                </td>
                            </tr>
                        <% end %>
                    <% end %>
                </tbody>
            </table>
            </div>
        </div>
        <div class="mt-3 mb-5">
            <%= f.submit 'Registrar Enólogo', class:'btn btn-primary' %>
        </div>
    <% end %>
</div>